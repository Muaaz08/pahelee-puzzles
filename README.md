# Pahelee Puzzles

This repo is split into two workspaces:

- `apps/web`: Vite, React, Tailwind, and web-only dependencies.
- `apps/app`: Capacitor config plus the Android and iOS native projects.

Run common commands from the repository root:

```sh
npm run dev
npm run build
npm run test
npm run cap:sync
```
