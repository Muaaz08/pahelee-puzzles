{
  "name": "pahelee-puzzles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "platform/web",
    "platform/app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @pahelee/web",
    "build": "npm run build --workspace @pahelee/web",
    "build:dev": "npm run build:dev --workspace @pahelee/web",
    "lint": "npm run lint --workspace @pahelee/web",
    "preview": "npm run preview --workspace @pahelee/web",
    "test": "npm run test --workspace @pahelee/web",
    "test:watch": "npm run test:watch --workspace @pahelee/web",
    "cap:sync": "npm run cap:sync --workspace @pahelee/app",
    "cap:android": "npm run cap:android --workspace @pahelee/app",
    "cap:ios": "npm run cap:ios --workspace @pahelee/app"
  }
}
